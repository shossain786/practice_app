import 'package:flutter/material.dart';

class MySampleLib extends StatelessWidget {
  const MySampleLib({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('SAMPLE APP'),
      ),
    );
  }
}
